services:
  - type: web
    name: car-research
    runtime: node
    plan: free
    autoDeploy: true
    repo: https://github.com/colindacity/car-research
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /cars
    envVars:
      - key: NODE_ENV
        value: production
